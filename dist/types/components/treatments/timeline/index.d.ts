/** Numbered steps linked left-to-right — a responsive row of Step cards (kind: steps). */
export declare const Timeline: import("../../runtime").TreatmentFactory<import("zod").ZodObject<{
    headline: import("zod").ZodString;
}, import("zod/v4/core").$strip>>;
