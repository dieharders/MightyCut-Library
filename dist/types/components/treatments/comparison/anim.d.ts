import type { AnimDescriptor } from "../../runtime/anim";
/** The headline rises in on the first VO line; each ledger row staggers after
 *  (the treatment offsets child anims to lines 1..N automatically). */
export declare const comparisonAnim: () => AnimDescriptor[];
