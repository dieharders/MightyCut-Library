/** Headline figures that count up — a responsive grid of Stat cards (kind: stats). */
export declare const StatGrid: import("../../runtime").TreatmentFactory<import("zod").ZodObject<{
    headline: import("zod").ZodString;
}, import("zod/v4/core").$strip>>;
